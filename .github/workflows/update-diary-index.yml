name: Update Writing Index

on:
  push:
    paths:
      - "public/writing/entries/*.md"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-index:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "18"

      - name: Update writing index.json
        run: |
          node <<EOF
          const fs = require('fs');
          const path = require('path');

          const entriesDir = 'public/writing/entries';
          const indexFile = 'public/writing/index.json';

          const files = fs.readdirSync(entriesDir).filter(file => file.endsWith('.md'));

          const entries = files.map(file => {
            const content = fs.readFileSync(path.join(entriesDir, file), 'utf-8');
            const metadataMatch = content.match(/<!--- metadata\s*([\s\S]+?)--->/);
            if (!metadataMatch) return null;

            const metadata = Object.fromEntries(
              metadataMatch[1]
                .split('\n')
                .filter(line => line.trim() && line.includes(':'))
                .map(line => {
                  const [key, ...valueParts] = line.split(':');
                  return [key.trim(), valueParts.join(':').trim()];
                })
            );

            return {
              title:    metadata.title    || '',
              date:     metadata.date     || '',
              slug:     file.replace('.md', ''),
              id:       metadata.id       || '',
              week:     metadata.week     || '',
              summary:  metadata.summary  || '',
              tags:     metadata.tags ? JSON.parse(metadata.tags) : [],
              featured: metadata.featured === 'true',
            };
          }).filter(entry => entry);

          // Sort by date descending (DD.MM.YYYY)
          entries.sort((a, b) => {
            const toMs = d => {
              const [dd, mm, yyyy] = d.split('.');
              return new Date(yyyy + '-' + mm + '-' + dd).getTime();
            };
            return toMs(b.date) - toMs(a.date);
          });

          fs.writeFileSync(indexFile, JSON.stringify(entries, null, 2));
          console.log('Updated ' + indexFile + ' with ' + entries.length + ' entries');
          EOF

      - name: Commit and push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add public/writing/index.json
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Update writing index.json"
            git push
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
